#include "main.h"

/**
 * main - This program will print the  alphabet 
 * in lower case follwed  by  a  new  line.
 *
 * Return: Always 0.
 */
void print_alphabet_*10(void)
{
	char l;
    	int n;

	while (n++ <=  9)
	{
		for (l = 'a'; l <= 'z';l++)
			_putchar('\n');
	}
    	return (0);
}
